---
id: common-operations
title: "Common Operations"
sidebar_label: Common Operations
slug: /guides/common-operations
description: The handful of endpoints that cover most day-to-day API work, grouped by domain.
---

# Common Operations

The SKU.io API has thousands of endpoints, but most integrations only ever touch a few dozen. This page is the shortlist — the create / read / update calls you'll reach for first in each domain, with the exact method, path, and required token scope.

Every operation name links to its full reference page (parameters, request body, response schema, and a live example). Each domain heading links to its section in the [API Reference](/docs/api/introduction), where you can browse the complete endpoint list.

:::tip New here?
Start with the [Quickstart](/docs/guides/quickstart) to make your first authenticated call, then skim [API Conventions](/docs/guides/api-conventions) for the `/api` vs `/api/v2` split and the response envelope. Scopes below (e.g. `orders:write`) map to token permissions — see [Authentication](/docs/guides/authentication).
:::

## Sales Orders

The core of order management: create orders, read them back, update, and dispatch shipments. → [Browse all Sales Orders endpoints](/docs/api/sales-orders)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Sales Orders](/docs/api/get-api-sales-orders-list) | `GET /api/sales-orders/list` | `orders:read` |
| [Create Sales Order](/docs/api/post-api-sales-orders) | `POST /api/sales-orders` | `orders:write` |
| [Get Sales Order](/docs/api/get-api-sales-orders-salesorder) | `GET /api/sales-orders/{salesOrder}` | `orders:read` |
| [Update Sales Order](/docs/api/put-api-sales-orders-salesorder) | `PUT /api/sales-orders/{salesOrder}` | `orders:write` |
| [Fulfill Sales Order](/docs/api/post-api-sales-orders-salesorder-fulfill) | `POST /api/sales-orders/{salesOrder}/fulfill` | `orders:write` |

## Fulfillment Orders

The dispatch side of fulfillment — the shipments warehouse and 3PL teams work off. → [Browse all Fulfillment Orders endpoints](/docs/api/fulfillment-orders)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Active Fulfillment Orders](/docs/api/get-api-fulfillment-orders) | `GET /api/fulfillment-orders` | `orders:read` |
| [Get Fulfillment Order](/docs/api/get-api-fulfillment-orders-fulfillmentorder) | `GET /api/fulfillment-orders/{fulfillmentOrder}` | `orders:read` |
| [Create Fulfillment Order](/docs/api/post-api-fulfillment-orders) | `POST /api/fulfillment-orders` | `orders:write` |

## Products

Your catalog. List and search products, create and update them, or look one up by SKU. → [Browse all Products endpoints](/docs/api/products)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Products](/docs/api/get-api-v-2-products) | `GET /api/v2/products` | `products:read` |
| [Create Product](/docs/api/post-api-products) | `POST /api/products` | `products:write` |
| [Get Product](/docs/api/get-api-products-id) | `GET /api/products/{id}` | `products:read` |
| [Update Product](/docs/api/put-api-products-id) | `PUT /api/products/{id}` | `products:write` |
| [Get Product by SKU](/docs/api/get-api-products-by-sku) | `GET /api/products/by-sku` | `products:read` |

## Inventory

Stock movement, adjustments, and allocation across warehouses. → [Browse all Inventory endpoints](/docs/api/inventory)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Inventory Movements](/docs/api/get-api-v-2-inventory-movements) | `GET /api/v2/inventory-movements` | `inventory:read` |
| [List Inventory Adjustments](/docs/api/get-api-v-2-inventory-adjustments) | `GET /api/v2/inventory-adjustments` | `inventory:read` |
| [Create Inventory Adjustment](/docs/api/post-api-inventory-adjustments) | `POST /api/inventory-adjustments` | `inventory:write` |
| [List Allocations](/docs/api/get-api-v-2-inventory-allocations) | `GET /api/v2/inventory-allocations` | `inventory:read` |

## Purchase Orders

Buying side: raise POs to suppliers, read them back, and track them through receipt. → [Browse all Purchase Orders endpoints](/docs/api/purchase-orders)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Purchase Orders](/docs/api/get-api-purchase-orders-list) | `GET /api/purchase-orders/list` | `purchase-orders:read` |
| [Create Purchase Order](/docs/api/post-api-purchase-orders) | `POST /api/purchase-orders` | `purchase-orders:write` |
| [Get Purchase Order](/docs/api/get-api-purchase-orders-purchase-order) | `GET /api/purchase-orders/{purchase_order}` | `purchase-orders:read` |

## Customers

Your contacts on the sell side. → [Browse all Customers endpoints](/docs/api/customers)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Customers](/docs/api/get-api-v-2-customers) | `GET /api/v2/customers` | `customers:read` |
| [Create Customer](/docs/api/post-api-customers) | `POST /api/customers` | `customers:write` |
| [Get Customer](/docs/api/get-api-customers-customer) | `GET /api/customers/{customer}` | `customers:read` |
| [Update Customer](/docs/api/put-api-customers-customer) | `PUT /api/customers/{customer}` | `customers:write` |

## Suppliers

Your contacts on the buy side. → [Browse all Suppliers endpoints](/docs/api/suppliers)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Suppliers](/docs/api/get-api-suppliers) | `GET /api/suppliers` | Public |
| [Create Supplier](/docs/api/post-api-suppliers) | `POST /api/suppliers` | `suppliers:write` |
| [Get Supplier](/docs/api/get-api-suppliers-supplier) | `GET /api/suppliers/{supplier}` | `suppliers:read` |

## Warehouses

Where inventory lives. → [Browse all Warehouses endpoints](/docs/api/warehouses)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Warehouses](/docs/api/get-api-v-2-warehouses) | `GET /api/v2/warehouses` | `warehouses:read` |
| [Create Warehouse](/docs/api/post-api-warehouses) | `POST /api/warehouses` | `warehouses:write` |

## Returns &amp; RMAs

Reverse logistics — return authorizations and their disposition. → [Browse all Returns &amp; RMAs endpoints](/docs/api/returns-rm-as)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List RMAs](/docs/api/get-api-rmas) | `GET /api/rmas` | `returns:read` |
| [Get RMA](/docs/api/get-api-rmas-rma) | `GET /api/rmas/{rma}` | `returns:read` |

## Accounting

Financial transactions and the ledger. → [Browse all Accounting endpoints](/docs/api/accounting)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Ledger Entries](/docs/api/get-api-ledger-entries) | `GET /api/ledger/entries` | Public |

## Webhooks

Get notified on events instead of polling. → [Browse all Webhooks endpoints](/docs/api/webhooks) · [Webhooks guide](/docs/guides/webhooks)

| Operation | Method &amp; Path | Scope |
|---|---|---|
| [List Webhooks](/docs/api/get-api-webhooks) | `GET /api/webhooks` | Public |
| [Create Webhook](/docs/api/post-api-webhooks) | `POST /api/webhooks` | Public |
| [List Event Types](/docs/api/get-api-webhook-events) | `GET /api/webhook-events` | `webhooks:manage` |

## Integrations

Connect and sync with sales channels and 3PLs. Each provider has its own folder in the reference:

- [Sales Channels overview](/docs/api/sales-channels) — the shared channel API
- [Amazon](/docs/api/amazon) · [Shopify](/docs/api/shopify) · [eBay](/docs/api/e-bay) · [Walmart](/docs/api/walmart) · [BigCommerce](/docs/api/big-commerce) · [WooCommerce](/docs/api/woo-commerce) · [TikTok Shop](/docs/api/tik-tok-shop)
- Accounting: [QuickBooks Online](/docs/api/quick-books-online) · [Xero](/docs/api/xero)
- 3PL / fulfillment: [ShipStation](/docs/api/ship-station) · [ShipHero](/docs/api/ship-hero) · [Shipfusion](/docs/api/shipfusion) · [Trackstar](/docs/api/trackstar)

---

Can't find what you need? The full [API Reference](/docs/api/introduction) is searchable, or point an AI assistant at the docs — see [Build with AI](/docs/guides/ai-tools).
